@{ Layout = null; }
<div ng-controller="SupplierPeopleCtrl">
    <table class="table table-striped table-bordered table-hover">
        <tr>
            <th>Id</th>
            <th>Name</th>
            <th></th>
        </tr>
        <tr ng-repeat="item in items">
            <td>{{item.PersonId}}</td>
            <td>{{item.FirstName}} {{item.LastName}}</td>
            <td>
                <div class="hidden-sm hidden-xs btn-group">
                    <button class="btn btn-danger btn-xs" ng-click="assign(item.PersonId)">
                        <i class="ace-icon glyphicon glyphicon-plus icon-only"></i>
                    </button>
                </div>
            </td>
        </tr>
    </table>
</div>

<script type="text/javascript">
    $(function () {
        $('#menu-hr').syncMenu([{ name: "supplier", option: "a" }], { title: 'Suppliers', link: '/Supplier' });
    });
</script>