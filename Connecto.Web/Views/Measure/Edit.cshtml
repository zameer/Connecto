@model Connecto.BusinessObjects.Measure

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Measure</legend>
        
        @Html.HiddenFor(model => model.MeasureId)
        <div class="editor-label">
            @Html.LabelFor(model => model.Lower)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Lower)
            @Html.ValidationMessageFor(model => model.Lower)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.Volume)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Volume)
            @Html.ValidationMessageFor(model => model.Volume)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.Actual)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Actual)
            @Html.ValidationMessageFor(model => model.Actual)
        </div>
        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

