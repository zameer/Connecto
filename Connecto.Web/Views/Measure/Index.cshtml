@model IEnumerable<Connecto.BusinessObjects.Measure>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Volume)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Lower)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Actual)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Volume)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Lower)
        </td>
        <td>
            1 @Html.DisplayFor(modelItem => item.Actual)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MeasureId }) |
            @Html.ActionLink("Details", "Details", new { id=item.MeasureId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MeasureId })
        </td>
    </tr>
}

</table>
